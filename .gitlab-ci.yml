variables:
  SAST_EXPERIMENTAL_FEATURES: "true"

image: $CI_IMAGE

stages:
  - build_doc

build doc:
  tags:
    - devops
  stage: build_doc
  script:
    - uname -r
    - cat /etc/os-release
  artifacts:
    expire_in: 24 hours
    paths:
      - build.zip